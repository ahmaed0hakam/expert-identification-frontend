# Keystore Configuration
# Copy this file to keystore.properties and fill in your actual values
# IMPORTANT: keystore.properties is gitignored - do NOT commit sensitive credentials!

# Path to your keystore file (can be absolute or relative to android directory)
# For paths with spaces, use the full path with quotes or escape spaces
# Example absolute path: /Users/ahmad/Desktop/key store.jks
# Example relative path: ../keystore/release.keystore
storeFile=/Users/ahmad/Desktop/key store.jks

# Keystore password
storePassword=your-keystore-password-here

# Key alias name (usually the same as the keystore name without extension)
keyAlias=your-key-alias-here

# Key password (usually the same as storePassword)
keyPassword=your-key-password-here
